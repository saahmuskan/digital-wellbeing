function SignInCreateAccountButton({ onClick }) {
  return (
    <button type="button" className="auth-signin-minimal" onClick={onClick}>
      Sign In (Create Account)
    </button>
  );
}

export default SignInCreateAccountButton;
